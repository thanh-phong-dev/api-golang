package users

import (
	"database/sql"

	"github.com/gin-gonic/gin"
)

func Routes(rg *gin.RouterGroup, db *sql.DB) {
	// ping := rg.Group("/user")
	// accountRoutes := ping.Group("/account")
	// {
	// 	account.Routes(accountRoutes)
	// }
}
